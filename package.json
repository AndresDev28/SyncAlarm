{
  "name": "syncalarm",
  "version": "0.1.0",
  "description": "SyncAlarm — sync native Android calendar events to device alarms. Conventional-Commit tooling for contributors.",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0"
  }
}
