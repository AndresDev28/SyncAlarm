# SDD Init Context — `sync-alarm`

> Generated by `sdd-init` on 2026-08-05. This is the canonical per-project
> init report. The same content is persisted in Engram under topic_key
> `sdd-init/sync-alarm` (project `syncalarm`).
>
> **Do not edit application code from this artifact.** It is context only.

## 1. Project Identity

| Field | Value |
| --- | --- |
| User-facing name | `sync-alarm` (engram canonical: `syncalarm`) |
| Git root | `/home/adreidev/dev/personal-projects/sync-alarm/SyncAlarm` |
| Remote | `https://github.com/AndresDev28/SyncAlarm.git` |
| Branch | `main` (HEAD: `f643186 Initial commit`) |
| Inner wrapper | `/home/adreidev/dev/personal-projects/sync-alarm/` (carries `opencode.json` with `subagent_depth: 2` and `.atl/skill-registry.md`) |
| CodeGraph | Initialized at inner `SyncAlarm/.codegraph/` (outer wrapper rejected init — no `.git/` there) |

## 2. Detected Stack

| Layer | Detected | Source of truth |
| --- | --- | --- |
| Language | **Kotlin** | PRD §6 "Plataforma: Android Nativo (Kotlin)" |
| Platform | **Android Native** (iOS explicitly out of scope per PRD §3) | PRD §3 / §6 |
| Architecture style | **Clean Architecture + MVVM** (Domain modules isolated from Android to enable TDD) | PRD §6 |
| Build tool | **Gradle (Kotlin DSL assumed)** | Android default; no `build.gradle(.kts)` exists yet |
| Package manager | **Gradle dependency resolution** (no `settings.gradle.kts` exists yet) | Inferred |
| Runtime | **Android** (min/target SDK not declared — to be chosen in sdd-design; recommend minSdk 26 / targetSdk 34) | PRD §3 implicit |
| Background scheduling | `WorkManager` (calendar scan) + `AlarmManager` (exact alarms via `setExactAndAllowWhileIdle`) | PRD §4 RF-06, §6 |
| Local persistence | `Room` or `DataStore` (decision deferred to sdd-design) | PRD §6 |
| Secret storage | `Jetpack Security` / `KeyStore` + `EncryptedSharedPreferences` | PRD §4 RF-01, §6 |
| Third-party | `Google Calendar API` via Google Play Services, OAuth2 PKCE | PRD §4 RF-01, §6 |
| DI (planned) | Not declared — recommend Hilt during sdd-design | Convention for Android + Clean Architecture |
| UI (planned) | Not declared — Jetpack Compose recommended (modern Android default); XML+ViewBinding acceptable | Convention |

### Critical Android Permissions (PRD §6)

- `android.permission.READ_CALENDAR`
- `android.permission.SCHEDULE_EXACT_ALARM`
- `android.permission.POST_NOTIFICATIONS`
- `android.permission.INTERNET`
- `android.permission.IGNORE_BATTERY_OPTIMIZATIONS` (via `ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`)

## 3. Conventions

**No conventions are currently enforced on disk.** Detection:

- No `.editorconfig`
- No `detekt.yml`, `detekt.gradle`, or `.ktlint` config
- No `gradle/libs.versions.toml` (version catalog)
- No `AGENTS.md`, `CLAUDE.md`, `.cursorrules`, `GEMINI.md`, or `copilot-instructions.md`
- No commit style proven by history (single `Initial commit`); persona defaults to **Conventional Commits**
- No naming pattern beyond the project folder `SyncAlarm/` (PascalCase root, snake-case-leaning docs)
- No directory layout yet — the project has only `README.md` and `docs/prd.md`

**Recommended conventions to add during the scaffold task** (not done by sdd-init):

- `.editorconfig` (Kotlin: 4-space indent, LF line endings, UTF-8, trim trailing whitespace)
- `detekt.yml` (baseline config) and `ktlint` hook
- `gradle/libs.versions.toml` (single source of truth for versions)
- `AGENTS.md` (project-level instruction file for AI agents — recommended given this is a multi-agent orchestrated repo)
- Conventional Commits via commitlint or commitizen

## 4. Architecture Pattern

Per PRD §6 and §3:

```
app/                       (Android module — Presentation + Application glue)
:app
├── presentation/          (Activities, ViewModels, Compose/UI)
├── di/                    (Hilt modules — TBD by sdd-design)
└── SyncAlarmApp.kt

domain/                   (Pure Kotlin module — no Android deps → TDD-friendly)
:domain
├── model/                (Alarm, CalendarEvent, Rule, Offset)
├── repository/           (interfaces only)
├── usecase/              (ScheduleAlarmUseCase, ResolveConflictsUseCase, …)
└── rules/                (RuleEngine, KeywordMatcher)

data/                     (Android module — implements domain repositories)
:data
├── calendar/             (CalendarProvider + Google Calendar API adapters)
├── alarm/                (AlarmManager wrapper, WorkManager workers)
├── persistence/          (Room DAO, DataStore, EncryptedSharedPreferences)
└── repository/           (implementations of domain.repository)
```

Dependency direction (strict): `app → domain ← data` and `app → data`. Domain has **zero** Android imports — this is the lever that unlocks TDD in the future.

## 5. Testing Capability

| Capability | Status | Notes |
| --- | --- | --- |
| Test runner | **Unavailable** | No `build.gradle.kts`, no `testImplementation` dependencies |
| Unit tests | ❌ | Will be `:domain` only (per architecture above) |
| Integration tests | ❌ | Will be `:data` with `androidx.test` + `MockK` |
| E2E | ❌ | Not planned for MVP (PRD makes no mention of UI tests) |
| Coverage tooling | ❌ | No `jacoco` or `kover` declared |
| Linter | ❌ | No `detekt`/`ktlint` |
| Type checker | ❌ | Kotlin compiler will handle (once Gradle exists) |
| Formatter | ❌ | No `ktlint`/`spotless` |
| **Strict TDD** | **`false`** | No test runner exists. Per sdd-init decision gate, strict TDD is `false` until a runner is detected. **Re-enable during `sdd-design` when Gradle + JUnit + (optionally) MockK/Turbine are declared.** |
| TDD command | `""` (none) | Will become `./gradlew test` once scaffolded |
| Test command | `""` (none) | Same |

## 6. Existing SDD Artifacts on Disk

| Path | Status |
| --- | --- |
| `openspec/` | **Created by sdd-init** (was absent) — `config.yaml`, `specs/`, `changes/`, `changes/archive/`, `context/sync-alarm.md` |
| `openspec/changes/` | Empty (no prior changes) |
| `openspec/config.yaml` | Created |
| `openspec/context/sync-alarm.md` | Created (this file) |
| `.atl/skill-registry.md` | Pre-existing (preserved, not regenerated — was current) |
| `.atl/.skill-registry.cache.json` | Pre-existing |
| `.codegraph/` | **Initialized by sdd-init** at the inner `SyncAlarm/` git root (outer wrapper rejected init because it has no `.git/` directory) |
| `sdd/` | Not used (project name is `sync-alarm`; openspec + engram carry the artifacts) |
| Engram observations | Created — see §7 |

## 7. Engram Artifacts Saved

| topic_key | type | scope | capture_prompt | content |
| --- | --- | --- | --- | --- |
| `sdd-init/sync-alarm` | `architecture` | `project` | `false` | This init report (project context, stack, conventions, testing capability, strict_tdd) |
| `sdd/sync-alarm/testing-capabilities` | `config` | `project` | `false` | Standalone testing capability snapshot (runner, layers, coverage, quality tools, strict_tdd) |

Engram project key (auto-detected from inner git remote): `syncalarm` (lowercase, no hyphen). The openspec folder keeps the user-facing `sync-alarm` name.

## 8. Persistence Mode

`both` (engram + openspec). Configured by orchestrator preflight. Hybrid per sdd-init decision gate.

## 9. Skill Resolution

`paths-injected` — the orchestrator injected the exact `sdd-init/SKILL.md` path in the task body. The pre-existing `.atl/skill-registry.md` was used as the source-of-truth index without regeneration.

## 10. Limitations / Risks

1. **No Kotlin/Gradle source exists yet.** Any SDD phase that calls `sdd-apply` will block until a scaffold bootstrap change creates the `build.gradle.kts`, `settings.gradle.kts`, `AndroidManifest.xml`, and the `:app`/`:domain`/`:data` module skeletons.
2. **Strict TDD is `false`.** The PRD plans a TDD-friendly domain layer, but the decision gate requires an actual test runner. The first scaffold task in `sdd-tasks` should add JUnit + MockK + Turbine + Turbine-coroutines and flip `openspec/config.yaml` `testing.strict_tdd` to `true` and `apply.tdd` to `true`.
3. **PRD is in Spanish** (the document at `SyncAlarm/docs/prd.md`). Generated technical artifacts (this file, engram observations, future specs/design/tasks) are in **English** by language-domain contract; the PRD is the source of truth for content, not for the artifact's working language.
4. **No commit style is proven.** Only `f643186 Initial commit` exists. Persona defaults to Conventional Commits. Establish a `commitlint` config during the scaffold task.
5. **Inner git repo is the real project root**, not the outer `sync-alarm/` wrapper. The wrapper carries `opencode.json` (`subagent_depth: 2` — this fixes the depth=1 problem from previous attempts) and `.atl/`. Any future agent that runs `codegraph_explore` should pass `projectPath: /home/adreidev/dev/personal-projects/sync-alarm/SyncAlarm`.
6. **No `.gitignore` at the inner `SyncAlarm/`** (the only `.gitignore` is the one CodeGraph created inside `.codegraph/`). Android Studio will normally generate one when the project is opened, but the scaffold task should commit an explicit one (`.gradle/`, `build/`, `local.properties`, `*.iml`, `.idea/`).
7. **The orchestrator must NOT advance to `sdd-explore`, `sdd-new`, or `sdd-continue` from this output.** sdd-init produces context only. The next step is to create a first change (recommended: `bootstrap-android-scaffold`) via `sdd-new` once the user gives the go-ahead.

## 11. Recommended Next Step

`sdd-new` with a first change name such as `bootstrap-android-scaffold` (creates Gradle/AndroidX/Hilt/JUnit/MockK skeleton, sets up Clean Architecture module boundaries, flips `strict_tdd` to `true`). Alternatively `sdd-explore` to validate PRD assumptions before any commitment — pick the one that matches the user's appetite for upfront validation vs. direct execution.
